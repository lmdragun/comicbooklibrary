<% if @comic.errors.any? %>
	<% @comic.errors.full_messages.each do |err| %>
			<p><%= err %></p>
		<% end %>
<% end %>

<%= form_for [@comic] do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :creators %>
	<%= f.text_field :creators %>
	<%= f.submit %>
<% end %>
